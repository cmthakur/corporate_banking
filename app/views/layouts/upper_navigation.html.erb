  <div class="navbar top_bar">
    <ul class="pull-right upper-nav">
      <% if user_signed_in? %>
      Welcome, <%= link_to current_user.email, edit_user_registration_path %> |
      <%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %>
      <% elsif member_signed_in? %>
      Welcome, <%= link_to current_member.email, edit_member_path(current_member) %> |
      <%= link_to 'Logout', destroy_member_session_path, :method=>'delete' %>
      <% else %>
      <%= link_to 'MemberLogin', new_member_session_path %>
      <% end %>
    </ul>
  </div>

