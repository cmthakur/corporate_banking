 <div class="app_title">
  <p> Co Operative Bank </p>
  <div class="navbar top_bar">
    <ul class="pull-right">
      <% if user_signed_in? %>
      Wecome, <%= link_to current_user.email, edit_user_registration_path %> |
      <%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %>
      <% elsif member_signed_in? %>
      Wecome, <%= link_to current_member.email, edit_member_registration_path %> |
      <%= link_to 'Logout', destroy_member_session_path, :method=>'delete' %>
      <% else %>
      <%= link_to 'MemberLogin', new_member_session_path %> |
      <%= link_to 'BankLogin', new_user_session_path %> |
      <%= link_to 'Sign up', new_user_registration_path %>
      <% end %>
    </ul>
  </div>
</div>
